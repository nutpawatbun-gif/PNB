# คู่มือการสำรองและฟื้นฟูระบบ (Backup & Disaster Recovery Guide)
## วิทยาลัยสงฆ์พ่อขุนผาเมือง เพชรบูรณ์ (MCU PKPM CMS)

คู่มือนี้สรุปกลไกการสำรองข้อมูล (Backup) และการกู้คืนระบบ (Restore/Disaster Recovery) สำหรับผู้ดูแลระบบสารสนเทศ วิทยาลัยสงฆ์พ่อขุนผาเมือง เพชรบูรณ์ มหาวิทยาลัยมหาจุฬาลงกรณราชวิทยาลัย

---

## 1. ประเภทของการสำรองข้อมูล (Backup Types)

ระบบ MCU PKPM มีระบบสำรองข้อมูลในตัว (Built-in Backup Manager) รองรับการสำรองข้อมูล 3 รูปแบบ:

| ประเภท | รายละเอียดขอบเขตข้อมูล | นามสกุลไฟล์ที่ได้ |
| :--- | :--- | :--- |
| **1. Database Snapshot** | สำรองเฉพาะโครงสร้างและข้อมูลตารางทั้ง 26 คอลเลกชัน (ข่าว, บุคลากร, ผลงาน, ผู้ใช้, ตั้งค่า) | `.json` |
| **2. Uploads Manifest** | สำรองรายการเมตาดาต้าและดรรชนีของไฟล์สื่อ/เอกสารในระบบทั้งหมด | `.json` |
| **3. Full System Snapshot** | สำรองทั้งข้อมูลฐานข้อมูลและสแนปชอตโครงสร้างไฟล์สื่อทั้งหมดในไฟล์เดียว | `.json` |

---

## 2. การสำรองข้อมูลผ่านเมนูผู้ดูแลระบบ (Admin Control Panel)

1. เข้าสู่ระบบด้วยบัญชี **Super Admin**
2. ไปที่เมนู **ตั้งค่าระบบ (Settings)** -> **สำรองข้อมูล (Backup Manager)**
3. เลือกรูปแบบที่ต้องการสำรองข้อมูล (`Database`, `Uploads Manifest`, หรือ `Full System`)
4. กรอกคำอธิบายเพิ่มเติม (เช่น *"สำรองข้อมูลก่อนอัปเดตหลักสูตรใหม่ปี 2569"*)
5. กดปุ่ม **สร้างไฟล์สำรองข้อมูล (Create Backup)**
6. ดาวน์โหลดไฟล์สำรองเก็บไว้ในเครื่องปลอดภัยหรือ Cloud Storage ผ่านปุ่ม **ดาวน์โหลด (Download Snapshot)**

---

## 3. การตั้งค่าระบบสำรองข้อมูลอัตโนมัติ (Automated Backup Schedule)

ในหน้า **Backup Manager** ผู้ดูแลระบบสามารถตั้งเวลาสำรองข้อมูลอัตโนมัติได้:

- **ความถี่**: รายวัน (Daily), รายสัปดาห์ (Weekly), หรือรายเดือน (Monthly)
- **เวลาทำงาน**: กำหนดเวลาระบบทำงาน เช่น `02:00` น.
- **นโยบายการเก็บรักษา (Retention Policy)**: 
  - กำหนดจำนวนวันจัดเก็บย้อนหลัง (เริ่มต้น 30 วัน)
  - กำหนดจำนวนไฟล์สแนปชอตสูงสุดที่เก็บ (เริ่มต้น 15 ไฟล์)
  - ระบบจะหมุนเวียนลบไฟล์สแนปชอตเก่าอัตโนมัติเมื่อเกินโควตาเพื่อประหยัดพื้นที่ดิสก์

---

## 4. การสำรองข้อมูลระดับ Command Line / Cron Job System

หากต้องการสำรองข้อมูลระดับ Linux Server โดยตรง สามารถตั้งค่า Cron Job ได้ดังนี้:

```bash
# สคริปต์สำรองข้อมูลดิสก์อัตโนมัติประจำวันเวลา 03:00 น.
crontab -e
```

เพิ่มบรรทัดคำสั่ง:
```cron
0 3 * * * tar -czf /var/backups/mcu_backup_$(date +\%Y\%m\%d).tar.gz /var/www/mcu-pkpm/data /var/www/mcu-pkpm/public/uploads
```

---

## 5. ข้นตอนการกู้คืนข้อมูล (System Restore Procedure)

### วิธีที่ 1: กู้คืนผ่านระบบหน้าเว็บ (GUI Restore)

1. เข้าสู่ระบบผู้ดูแลระบบ เลือกเมนู **Backup Manager**
2. ในส่วน **คืนค่าข้อมูล (Restore Database)** เลือกไฟล์ `.json` ที่เคยสำรองไว้
3. หรือเลือกไฟล์สแนปชอตจากตารางประวัติการสำรอง แล้วกดปุ่ม **กู้คืน (Restore)**
4. ระบบจะตรวจสอบความถูกต้องของไฟล์ JSON
5. ยืนยันการกู้คืน ข้อมูลในระบบจะถูกย้อนกลับไปยังสแนปชอตนั้นทันที

### วิธีที่ 2: กู้คืนระดับไฟล์ (Disaster Recovery via File System)

กรณีระบบไม่สามารถเปิดหน้าเว็บได้ สามารถนำไฟล์สแนปชอตเขียนทับลงในฐานข้อมูลโดยตรง:

```bash
# 1. หยุดการทำงานของ Node.js Service
pm2 stop mcu-pkpm-app

# 2. คัดลอกไฟล์สำรองมาแทนที่ฐานข้อมูลเดิม
cp /path/to/backup_snapshot.json /var/www/mcu-pkpm/data/mcu_database.json

# 3. กำหนดสิทธิ์ให้ถูกต้อง
chown www-data:www-data /var/www/mcu-pkpm/data/mcu_database.json
chmod 644 /var/www/mcu-pkpm/data/mcu_database.json

# 4. รีสตาร์ทบริการ
pm2 restart mcu-pkpm-app
```

---

## 6. ข้อควรระวังและการรักษาความปลอดภัยไฟล์สำรอง

1. **ห้ามเผยแพร่ไฟล์สำรองข้อมูล**: ไฟล์ `.json` มีรหัสแฮชและข้อมูลสำคัญขององค์กร
2. **ทดสอบกู้คืนสม่ำเสมอ**: ควรทดสอบขั้นตอนการกู้คืนอย่างน้อยทุกๆ 6 เดือน
3. **จัดเก็บต่างสถานที่ (Off-site Backup)**: ควรคัดลอกไฟล์สำรองไปเก็บไว้บนความจุภายนอก เช่น Secure Cloud Drive ของมหาวิทยาลัย
